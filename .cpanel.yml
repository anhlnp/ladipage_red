---
deployment:
  tasks:
    - export DEPLOYPATH=/home/lbb6fey2urjf/public_html/demo.selecttechinc.com/
    # Force reset to match remote exactly (avoid diverging branch error)
    - /usr/local/cpanel/3rdparty/bin/git fetch origin
    - /usr/local/cpanel/3rdparty/bin/git reset --hard origin/build
    - /bin/cp -R * $DEPLOYPATH
